apiVersion: v1
kind: Service
metadata:
    name: "servicedeliveryproductzero-a3fc"
    labels:
        app: "servicedeliveryproductzero-a3fc"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "servicedeliveryproductzero-a3fc"